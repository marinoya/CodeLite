#include <iostream>
#include <map>
using namespace std;

vector<int> numCouner(int arr[]);

int main (){
	cout << "Hello world" << endl;
	
	map<int, int> m1 {};
	
	return 0;
	}
	
	
	vector<int> numCouner(int arr[], size_t size)
	{
		for (size_t i{0};i<size; ++i)
		{
			if( m1.count(arr[i]) )
			{
				m1.insert(arr[i]] = 1;
			}else{
		}
	}